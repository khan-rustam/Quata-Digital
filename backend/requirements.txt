# Core (works on Python 3.11 – 3.14)
fastapi>=0.118.0
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.36
alembic>=1.14.0
pydantic>=2.10.5
pydantic-settings>=2.7.0
# Maintained JWT library. python-jose has been unmaintained since 2024
# (CVE-2024-33663, CVE-2024-33664 — JWT-bomb / algorithm-confusion).
PyJWT>=2.9.0
bcrypt>=4.2.0
# Upper bound keeps us inside the patched 0.0.20 series until we
# explicitly review the next minor for breaking changes.
python-multipart>=0.0.20,<0.1
email-validator>=2.2.0
websockets>=14.0

# Security: 2FA + rate limiting
pyotp>=2.9.0
qrcode>=7.4.0
slowapi>=0.1.9
# Encrypts TOTP secrets at rest (services/security_extras). Usually present
# transitively; pinned explicitly since we import it directly.
cryptography>=42.0.0

# Image pipeline (dimensions + WebP optimisation for uploads)
Pillow>=10.4.0

# Background queue (only used when REDIS_URL is set; the queue helper falls
# back to synchronous in-process execution otherwise)
redis>=5.0.0
rq>=1.16.0

# Optional S3 / R2 / MinIO upload backend (only loaded when
# UPLOAD_BACKEND=s3). Default upload backend stays local-disk; this stays
# unused on single-VPS deployments without paying any import cost.
boto3>=1.35.0,<2

# Observability (Sentry is opt-in via SENTRY_DSN env var)
python-json-logger>=3.2.0
sentry-sdk>=2.18.0

# Postgres driver — bundled by default so the production image can talk
# to a managed Postgres without an extra `pip install` step at deploy
# time. SQLite path still works for local dev when DATABASE_URL is unset.
psycopg[binary]>=3.2.3

# AI talent-intelligence engine (HRMS 1E). Only used when OPENAI_API_KEY is
# set — the openai client is imported lazily so the app boots without a key.
# pypdf extracts text from PDF CVs before analysis.
openai>=1.40.0
pypdf>=4.2.0
